#ifndef EPICS_DB_PARSE_H
#define EPICS_DB_PARSE_H

#include <map>
#include <string>
#include <queue>

class EpicsDbParse
{
    public:
    private:
};

short check_calc_eq(std::string pinfix);

#endif
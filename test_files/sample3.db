###
# /bug There are no reported bugs. I mean come on. There has to be a bug somewhere! I think I like to watch the stars.
# What the heck is going on.
#
# /file sample3.db
# /brief This is a brief description of what the records in this database file will do. There are several
# things it does.
# 
# /desc This constant is defined with a value of -1, which because size_t is an unsigned integral type, it is the largest possible representable 
# value for this type. 
###

###  
# /bug There are no reported bugs. I mean come on. There has to be a bug somewhere! I think I like to watch the stars.
#
# /brief This is a brief description of how current get works.
#
# /desc This detects the scope error.
###

record (((ai, "PS1:I-get")
{
    field (DESC, "Read current of PS1") field (DTYP, "stream") 
    field (INP,  "@ExamplePS.proto getCurrent PS1")
    field (EGU,  "A")
}

###  
# /bug There are no reported bugs. I mean come on. There has to be a bug somewhere! I think I like to watch the stars.
#
# /brief This is a brief description of how current set works.
#
# /desc This detects the scope error.
###

record (ao, "PS1:I-set")
{
    field (DESC, "Set current of PS1")
    field (DTYP, "stream")
    field (OUT,  "@ExamplePS.proto setCurrent PS1")
    field (EGU,  "A")
    field (PREC, "2")
}